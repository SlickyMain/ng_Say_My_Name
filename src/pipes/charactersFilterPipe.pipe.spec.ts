/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CharactersFilterPipePipe } from './charactersFilterPipe.pipe';

describe('Pipe: CharactersFilterPipee', () => {
  it('create an instance', () => {
    let pipe = new CharactersFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
